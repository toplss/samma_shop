<?php

return [

    /*
    |--------------------------------------------------------------------------
    | SSO 서버 URL
    |--------------------------------------------------------------------------
    |
    | SSO 요청을 보낼 서버 기본 URL을 설정합니다.
    |
    */
    'url' => env('SSO_URL', 'https://sso.example.com/mall'),

    /*
    |--------------------------------------------------------------------------
    | API 키 또는 토큰
    |--------------------------------------------------------------------------
    |
    | SSO 서버에서 인증용으로 사용하는 키가 있다면 여기에 등록
    |
    */
    'api_key' => env('SSO_API_KEY', null),

    /*
    |--------------------------------------------------------------------------
    | 요청 타임아웃
    |--------------------------------------------------------------------------
    |
    | HTTP 요청 시 타임아웃을 설정합니다. (초 단위)
    |
    */
    'timeout' => env('SSO_TIMEOUT', 5),

    /*
    |--------------------------------------------------------------------------
    | 기타 옵션
    |--------------------------------------------------------------------------
    |
    | 필요하면 여기에 추가 설정 가능
    |
    */
    'some_option' => env('SSO_OPTION', true),
];
